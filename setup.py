#!/usr/bin/env python3
from distutils.core import setup
args = {
    'name': 'timed_input',
    'version': '0.02',
    'description': 'A library for handling timeout inputs',
    'python_requires': '>=3.0',
    'packages': ['timed_input']
}

setup(**args)
