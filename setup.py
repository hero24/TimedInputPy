#!/usr/bin/env python3
from distutils.core import setup
args= {
        "name" : "TimedInput",
        "version" : "0.01",
        "description" : "A library for handling timeout inputs",
        "python_requires": ">=3.0",
        "packages" : ["TimedInput"]
}

setup(**args)
